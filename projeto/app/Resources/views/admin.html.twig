{% extends '::base.html.twig' %}

{% block navbar %}
<header class="main-header">
    <nav class="navbar navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <img src="http://evsdesucesso.com.br/cliente/img/logo-top.png">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
            <i class="fa fa-bars"></i>
          </button>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbar-collapse">
          <ul class="nav navbar-nav navbar-right">
            <li class="{% if app.request.attributes.get('_route') ends with 'anfitrioes' %} active {% endif %}">
                <a href="{{ path('admin_anfitrioes') }}">
                    <i class="fa fa-users"></i>
                    <span class="titulo">Anfitriões</span>
                </a>
            </li>
    
            <!--{% if is_granted('ROLE_SUPER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'categorias' %} active {% endif %}">
                <a href="{{ path('admin_categorias') }}">
                    <i class="fa fa-sitemap"></i>
                    <span class="titulo">Categorias</span>
                </a>
            </li>
            {% endif %}-->

            {% if is_granted('ROLE_SUPER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'tags' %} active {% endif %}">
                <a href="{{ path('admin_tags') }}">
                    <i class="fa fa-tags"></i>
                    <span class="titulo">Tags</span>
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_SUPER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'produtos' %} active {% endif %}">
                <a href="{{ path('admin_produtos') }}">
                    <i class="fa fa-shopping-bag"></i>
                    <span class="titulo">Produtos</span>
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_SUPER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'grade-consumo' %} active {% endif %}">
                <a href="{{ path('admin_grade_consumo') }}">
                        <i class="fa fa-table"></i>
                    <span class="titulo">Grade</span>
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_OWNER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'grade-consumo-pdv' %} active {% endif %}">
                <a href="{{ path('admin_grade_consumo_pdv') }}">
                    <i class="fa fa-table"></i>
                    <span class="titulo">Grade PDV</span>
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_SUPER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'precos' %} active {% endif %}">
                <a href="{{ path('admin_precos') }}">
                    <i class="fa fa-bullhorn"></i>
                    <span class="titulo">Tabela</span>
                </a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_SUPER') or is_granted('ROLE_OWNER') or is_granted('ROLE_MASTER') %}
            <li class="{% if app.request.attributes.get('_route') ends with 'pdv' %} active {% endif %}">
                <a href="{{ path('admin_pdv') }}">
                    <i class="fa fa-bullhorn"></i>
                    <span class="titulo">PDV</span>
                </a>
            </li>
            {% endif %}

            <li class="dropdown">
                <a href="#!" class="dropdown-toggle" data-toggle="dropdown" data-hover="dropdown">
                  <i class="fa fa-bar-chart-o"></i>
                  Relatórios
                  <span class="caret"></span>
                </a>

                <ul class="dropdown-menu">
                  <li><a href="{{ path('admin_relatorio_detalhado_vendas_diario') }}" class="orange"><i class="fa fa-list-alt nav-icon"></i> Detalhado de Vendas</a></li>
                  <li><a href="{{ path('admin_relatorio_detalhado_atividades_diario') }}" class="gray"><i class="fa fa-list-alt nav-icon"></i> Diário Detalhado de Atividade</a></li>
                  <li><a href="{{ path('admin_relatorio_mensal_ganho') }}" class="green"><i class="fa fa-list-alt nav-icon"></i> Mensal de Ganhos</a></li>
                </ul>
            </li>
            
            <li>
                <a href="{{ logout_path() }}">
                    <i class="fa fa-sign-out"></i>
                    <span class="titulo">Sair</span>
                </a>
            </li>
          </ul>
          
        </div>
        
      </div>
      <!-- /.container-fluid -->
    </nav>
  </header>
{% endblock %}

{% block body_class %} skin-blue sidebar-mini {% endblock %}

{% block sidebar %}
    <aside class="main-sidebar">
        <section class="sidebar" style="height: auto;">
            <ul class="sidebar-menu">
                <li class="header">MENU PRINCIPAL</li>
				<li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Anfitriões</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_anfitrioes') }}">Listar e gerenciar anfitriões</a></li>
                    </ul>
                </li>
                <li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Categorias</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_categorias') }}">Listar e gerenciar categorias</a></li>
                    </ul>
                </li>
                <li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Tags</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_tags') }}">Listar e gerenciar tags</a></li>
                    </ul>
                </li>
                <li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Produtos</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_produtos') }}">Listar e gerenciar produtos</a></li>
                    </ul>
                </li>
                <li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Grade Consumo</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_grade_consumo') }}">Listar e gerenciar grade</a></li>
                    </ul>
                </li>
                <li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Tabela de Preços</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_precos') }}">Listar e gerenciar tabela</a></li>
                    </ul>
                </li>
                <li class="active treeview">
                    <a href="#">
                        <i class="material-icons">group</i><span>Ponto de Venda</span>
                    </a>
                    <ul class="treeview-menu">
                        <li><a href="{{ path('admin_pdv') }}">Listar e gerenciar pdv</a></li>
                    </ul>
                </li>
                </ul>
        </section>
        <!-- /.sidebar -->
    </aside>
{% endblock %}

{% block content %}
    <div class="content-wrapper" style="min-height: 901px;">
        <div class="container" style="width: 100%; padding: 0;">
            <section class="content-header">
                <h1><i class="material-icons"></i></h1>
            </section>
            <section class="content">

                {# Conteúdo #}
                {% block body %}{% endblock %}

            </section>
        </div>
    </div>
{% endblock %}

{% block footer %}
    <footer class="main-footer">
        <div class="container text-center">
            Copyright 2016 EVS. Todos os direitos reservados. Desenvolvido por <a href="http://cm2tech.com.br">CM2 Tech</a>.
        </div>
    </footer>
{% endblock %}